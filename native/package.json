{
  "name": "chinese-learning-native",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for the Chinese Learning PWA",
  "scripts": {
    "sync": "cap sync android",
    "copy": "cap copy android",
    "open": "cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.7",
    "@capacitor/cli": "^7.6.7",
    "@capacitor/core": "^7.6.7"
  }
}
