{
  "name": "chinese-learning-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply chinese-learning-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply chinese-learning-db"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}
